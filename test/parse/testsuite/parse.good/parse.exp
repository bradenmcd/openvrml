proc run_parse_tests { worlds } {
    foreach world $worlds {
        set test $world
        if [catch { parse_start $world }] {
            fail "$test"
        } else {
            pass "$test"
        }
    }
}

run_parse_tests [lsort [glob $srcdir/$subdir/*.wrl]]
